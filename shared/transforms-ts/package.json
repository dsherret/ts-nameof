{
  "name": "ts-nameof-transforms-ts",
  "private": true,
  "version": "0.0.0",
  "description": "TypeScript compiler transforms for ts-nameof packages.",
  "main": "./dist/index.js",
  "author": "David Sherret",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc --build",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "test": "yarn run build && nyc --reporter=lcov mocha --opts mocha.opts"
  },
  "dependencies": {
    "ts-nameof-common": "file:../common"
  },
  "devDependencies": {
    "ts-nameof-tests-common": "file:../tests-common",
    "ts-node": "^8.0.1",
    "typescript": "^3.1.6",
    "coveralls": "^3.0.2",
    "mocha": "^5.2.0",
    "nyc": "^13.1.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0"
  }
}
