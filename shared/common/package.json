{
  "name": "ts-nameof-common",
  "private": true,
  "version": "0.0.0",
  "description": "Common code across packages.",
  "main": "./dist/index.js",
  "author": "David Sherret",
  "license": "MIT",
  "scripts": {
    "lint": "tslint -c tslint.json src/**/*.ts",
    "lint:fix": "tslint --fix -c tslint.json src/**/*.ts",
    "build": "tsc",
    "install-build": "yarn install && yarn build"
  },
  "dependencies": {
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
