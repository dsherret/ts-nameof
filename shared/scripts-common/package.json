{
  "name": "ts-nameof-scripts-common",
  "private": true,
  "version": "0.0.0",
  "description": "Common scripts for ts-nameof packages.",
  "main": "./dist/index.js",
  "author": "David Sherret",
  "license": "MIT",
  "scripts": {
    "lint": "tslint -c tslint.json src/**/*{.ts,.js}",
    "lint:fix": "tslint --fix -c tslint.json src/**/*{.ts,.js}",
    "build": "tsc",
    "install-build": "yarn install && yarn build"
  },
  "devDependencies": {
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.6.8",
    "@types/prettier": "^1.18.0",
    "prettier": "^1.18.2",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
