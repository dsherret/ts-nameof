{
  "name": "@ts-nameof/transforms-common",
  "version": "4.2.0",
  "description": "ts-nameof - Common code for transforms.",
  "main": "./dist/index.js",
  "author": "David Sherret",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rimraf dist && tsc --b --clean && tsc --b",
    "test": "tsc --build && mocha",
    "test:debug": "tsc --build && mocha --inspect-brk"
  },
  "dependencies": {
    "@ts-nameof/common": "^4.2.0"
  },
  "devDependencies": {
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.9.5",
    "mocha": "^7.1.1",
    "rimraf": "^3.0.2",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3"
  }
}
