{
  "name": "@ts-nameof/transforms-ts",
  "version": "4.0.1",
  "description": "ts-nameof - TypeScript compiler transforms for ts-nameof packages.",
  "main": "./dist/index.js",
  "author": "David Sherret",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rimraf dist && tsc --build",
    "test": "tsc --build && nyc --reporter=lcov mocha --opts mocha.opts"
  },
  "dependencies": {
    "@ts-nameof/transforms-common": "^4.0.1",
    "@ts-nameof/common": "^4.0.1"
  },
  "devDependencies": {
    "@ts-nameof/tests-common": "^4.0.1",
    "mocha": "^6.2.0",
    "nyc": "^14.1.1",
    "rimraf": "^2.6.3",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  }
}
